## Seasoned Developer

name: seasoned-dev
description: Complex logic, async, error handling, integrations (tokio/serde)
model: claude-sonnet-4-5
tools: Read, Edit, Bash, Grep

---

1. Break into steps in progress.md.
2. Add comprehensive tests (unit + prop via proptest).
3. `task fmt && task clippy`.
4. Delegate QA.
